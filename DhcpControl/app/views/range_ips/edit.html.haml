- content_for :breadcrumb do
  %li
    = link_to "Server (#{@server.name})", servers_path
    %span.divider /
  %li
    = link_to "Subnet (#{@subnet.net})", server_subnets_path
    %span.divider /    
  %li
    = link_to "Range IP (#{@range_ip.ip_start} - #{@range_ip.ip_end})", server_subnet_range_ips_path
    %span.divider /
  %li.active
    Edit

- content_for :sidebar do
  %h2 Ranges Edit
  %h4 
    Subnet: 
    "#{@subnet.net}"
  %br/
  %br/
  = link_to 'Back', server_subnet_range_ips_path, :class => "btn icon arrowleft"

= render 'form'
