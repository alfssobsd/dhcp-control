- content_for :breadcrumb do
  %li
    = link_to "Server (#{@server.name})", servers_path
    %span.divider /
  %li
    = link_to "Subnet (#{@subnet.net})", server_subnets_path
    %span.divider /
  %li
    = link_to "DDNS Zone (#{@ddns_zone.name})", server_subnet_ddns_zones_path
    %span.divider /
  %li
    Show


- content_for :sidebar do
  %h2 DDNS Zone Show
  %h4 
    Subnet: 
    "#{@subnet.net}"
    
%p#notice= notice
%p
  %b Subnet:
  = @ddns_zone.subnet_id
%p
  %b Name:
  = @ddns_zone.name
%p
  %b Primary:
  = @ddns_zone.primary
%p
  %b Ddns key:
  = @ddns_zone.ddns_key_id
= link_to 'Edit', edit_server_subnet_ddns_zone_path(@server, @subnet, @ddns_zone)
|
\#{link_to 'Back', server_subnet_ddns_zones_path}
